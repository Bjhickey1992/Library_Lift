# API keys and secrets (do not commit)
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so
.eggs/
*.egg-info/
*.egg

# Weekly Phase 2 run state (local only)
.last_weekly_phase2_run
.weekly_phase2_running

# Generated exhibition embeddings (regenerate with Phase 2)
upcoming_exhibitions_embeddings.npy
upcoming_exhibitions_embeddings.xlsx

# Local copies / raw (optional to commit)
upcoming_exhibitions_copy.xlsx
upcoming_exhibitions_raw.xlsx

# Excel lock / temp (Windows)
~$*.xlsx
~$*.xls

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
